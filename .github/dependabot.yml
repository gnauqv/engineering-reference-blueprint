# Phiên bản cấu hình của Dependabot
version: 2
updates:
  # Tự động quét cập nhật cho các GitHub Actions workflows
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      # Tần suất kiểm tra hàng tuần
      interval: "weekly"
